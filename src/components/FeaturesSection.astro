---
---

<section class="features-section px-6 md:px-12 py-24 md:py-36" aria-labelledby="features-heading">

  <div class="section-label mb-12">
    <span class="font-jet text-xs uppercase tracking-[0.25em] text-black/40 dark:text-white/50">
      · Características
    </span>
    <div class="label-line" aria-hidden="true"></div>
  </div>

  <h2 id="features-heading" class="font-departure text-3xl md:text-5xl uppercase text-black dark:text-white mb-16 max-w-3xl leading-tight">
    Todo lo que necesitas<br /><span class="text-primary">para practicar</span>
  </h2>

  <div class="features-grid">

    <article class="feature-card feature-large">
      <div class="feature-icon" aria-hidden="true">
        <span class="font-geistGrid text-4xl text-primary">◼</span>
      </div>
      <h3 class="font-departure text-xl uppercase text-black dark:text-white mb-2">Chat con IA generativa</h3>
      <p class="font-jet text-sm text-black/60 dark:text-white/60 leading-relaxed">
        Groq + Cerebras generan mensajes contextualmente relevantes para tu juego o tema. No son frases genéricas: son comentarios que realmente dirían espectadores reales.
      </p>
    </article>

    <article class="feature-card">
      <div class="feature-icon" aria-hidden="true">
        <span class="font-geistCircle text-3xl text-primary">●</span>
      </div>
      <h3 class="font-departure text-base uppercase text-black dark:text-white mb-2">Emotes 7TV globales</h3>
      <p class="font-jet text-xs text-black/60 dark:text-white/60 leading-relaxed">
        Integración con la API pública de 7TV. Los emotes aparecen naturalmente en los mensajes.
      </p>
    </article>

    <article class="feature-card">
      <div class="feature-icon" aria-hidden="true">
        <span class="font-geistLine text-3xl text-primary">▬</span>
      </div>
      <h3 class="font-departure text-base uppercase text-black dark:text-white mb-2">Velocidad ajustable</h3>
      <p class="font-jet text-xs text-black/60 dark:text-white/60 leading-relaxed">
        Desde chat tranquilo hasta hype máximo. Cuatro presets de velocidad para simular cualquier tamaño de audiencia.
      </p>
    </article>

    <article class="feature-card platform-card">
      <div class="platform-badges" aria-label="Plataformas compatibles">
        <span class="platform-badge twitch font-departure">Twitch</span>
        <span class="platform-badge kick font-departure">Kick</span>
      </div>
      <h3 class="font-departure text-base uppercase text-black dark:text-white mb-2 mt-4">Twitch & Kick</h3>
      <p class="font-jet text-xs text-black/60 dark:text-white/60 leading-relaxed">
        Cambia de plataforma con un click. El tema visual completo se adapta al estilo de cada una.
      </p>
    </article>

    <article class="feature-card feature-wide">
      <div class="feature-icon" aria-hidden="true">
        <span class="font-geistSquare text-3xl text-primary">■</span>
      </div>
      <h3 class="font-departure text-base uppercase text-black dark:text-white mb-2">Just Chatting mode</h3>
      <p class="font-jet text-xs text-black/60 dark:text-white/60 leading-relaxed">
        No solo para gaming. Practica charlas de vida, música, IRL o cualquier tema libre. La IA adapta el tono del chat automáticamente.
      </p>
    </article>

  </div>

</section>

<style>
  /* ─── Section label ────────────────────────── */
  .section-label {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }
  .label-line {
    flex: 1;
    height: 0.5px;
    background: rgba(0,0,0,0.12);
    max-width: 12rem;
  }
  @media (prefers-color-scheme: dark) {
    .label-line { background: rgba(255,255,255,1); }
  }

  /* ─── Features section ─────────────────────── */
  .features-section {
    border-top: 0.5px solid rgba(0,0,0,0.08);
  }
  @media (prefers-color-scheme: dark) {
    .features-section { border-top-color: rgba(255,255,255,0.06); }
  }

  .features-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1px;
    background: rgba(0,0,0,0.2);
  }
  @media (prefers-color-scheme: dark) {
    .features-grid { background: rgba(255,255,255,0.2); }
  }
  @media (min-width: 640px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (min-width: 1024px) {
    .features-grid {
      grid-template-columns: 1.5fr 1fr 1fr;
      grid-template-rows: auto auto;
    }
    .feature-large {
      grid-row: span 2;
    }
    .feature-wide {
      grid-column: span 2;
    }
  }

  .feature-card {
    background: var(--color-bg-secundary);
    padding: 2rem;
  }
  @media (prefers-color-scheme: dark) {
    .feature-card { background: #000; }
  }

  .feature-icon {
    margin-bottom: 1rem;
    line-height: 1;
  }

  .platform-badges {
    display: flex;
    gap: 0.5rem;
  }
  .platform-badge {
    padding: 0.25rem 0.75rem;
    font-size: 0.75rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
  .platform-badge.twitch {
    background: #9146FF;
    color: #fff;
  }
  .platform-badge.kick {
    background: #53FC18;
    color: #0a1a00;
  }
</style>
