---
---

<section class="how-section px-6 md:px-12 py-24 md:py-36" aria-labelledby="how-heading">

  <div class="section-label mb-12">
    <span class="font-jet text-xs uppercase tracking-[0.25em] text-black dark:text-white/50">
      <span class="text-black">01</span> · Cómo funciona
    </span>
    <div class="label-line" aria-hidden="true"></div>
  </div>

  <h2 id="how-heading" class="font-departure text-3xl md:text-5xl uppercase text-black dark:text-white  mb-16 max-w-3xl leading-tight">
    tres sencillos pasos <br />
  </h2>

  <div class="steps-grid">

    <article class="step-card" data-step="01">
      <div class="step-number font-departure text-primary" aria-hidden="true">01</div>
      <div class="step-content">
        <h3 class="font-departure text-lg uppercase text-black dark:text-white mb-3">Elige tu juego</h3>
        <p class="font-jet text-sm text-black/60 dark:text-white/60 leading-relaxed">
          Escribe el nombre del juego que vas a streamear o selecciona el modo Just Chatting. La IA genera frases específicas del contexto.
        </p>
      </div>
      <div class="step-connector" aria-hidden="true"></div>
    </article>

    <article class="step-card" data-step="02">
      <div class="step-number font-departure text-primary" aria-hidden="true">02</div>
      <div class="step-content">
        <h3 class="font-departure text-lg uppercase text-black dark:text-white mb-3">Activa el chat</h3>
        <p class="font-jet text-sm text-black/60 dark:text-white/60 leading-relaxed">
          Lanza el stream simulado y el chat empieza a fluir. Usernames de colores, timestamps reales y emotes de 7TV incluidos.
        </p>
      </div>
      <div class="step-connector" aria-hidden="true"></div>
    </article>

    <article class="step-card" data-step="03">
      <div class="step-number font-departure text-primary" aria-hidden="true">03</div>
      <div class="step-content">
        <h3 class="font-departure text-lg uppercase text-black dark:text-white mb-3">Practica y crece</h3>
        <p class="font-jet text-sm text-black/60 dark:text-white/60 leading-relaxed">
          Ajusta la velocidad del chat. Responde. Acostúmbrate al ritmo. Cuando vayas en vivo, ya lo habrás hecho antes.
        </p>
      </div>
    </article>

  </div>

</section>

<style>
  /* ─── Section label ────────────────────────── */
  .section-label {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }
  .label-line {
    flex: 1;
    height: 0.5px;
    background: rgba(0,0,0,1);
    max-width: 12rem;
  }
  @media (prefers-color-scheme: dark) {
    .label-line { background: rgba(255,255,255,0.2); }
  }

  /* ─── How section ──────────────────────────── */
  .how-section {
    border-top: 0.5px solid rgba(0,0,0,0.08);
    max-width: 100%;
  }
  @media (prefers-color-scheme: dark) {
    .how-section { border-top-color: rgba(255,255,255,0.1); }
  }

  .steps-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0;
  }
  @media (min-width: 768px) {
    .steps-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .step-card {
    padding: 2.5rem 0;
    border-top: 0.5px solid rgba(0,0,0,1);
    position: relative;
  }
  @media (prefers-color-scheme: dark) {
    .step-card { border-top-color: rgba(255,255,255,0.5); }
  }
  @media (min-width: 768px) and (prefers-color-scheme: dark) {
    .step-card { border-left-color: rgba(255,255,255,0.5); }
  }
  @media (min-width: 768px) {
    .step-card {
      padding: 2.5rem 2.5rem 2.5rem 0;
      border-top: none;
      border-left: 0.5px solid rgba(0,0,0,1);
      padding-left: 2.5rem;
    }
    .step-card:first-child {
      border-left: none;
      padding-left: 0;
    }
  }
  @media (min-width: 768px) and (prefers-color-scheme: dark) {
    .step-card { border-left-color: rgba(255,255,255,0.2); }
  }

  .step-number {
    font-size: 3.5rem;
    line-height: 1;
    margin-bottom: 1.25rem;

  }
</style>
